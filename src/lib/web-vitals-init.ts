// Optional: Enable Web Vitals tracking
// Uncomment the following lines to enable performance monitoring in production

// import { reportWebVitals } from '@/lib/web-vitals';

// // Start tracking Web Vitals
// if (import.meta.env.PROD) {
//   reportWebVitals();
// }

// // Optional: Track performance metrics on page load
// if (import.meta.env.DEV) {
//   window.addEventListener('load', () => {
//     const metrics = import('@/lib/web-vitals').then(({ getPerformanceMetrics }) => {
//       const metrics = getPerformanceMetrics();
//       console.log('[Performance Metrics]', metrics);
//     });
//   });
// }
